# Fertige Dateien (Produced Files)

In diesem Ordner werden alle vom System generierten Audio-Dateien (`.wav`) zusammen mit ihrer Konfigurations- und Log-Datei (`_config.txt`) automatisch gespeichert und archiviert.
